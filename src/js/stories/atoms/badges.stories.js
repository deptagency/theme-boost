import React from 'react'

export default {
    title: 'Start storybook',
}

export const start = () => {
    return (
        <div>
            Storybook works
        </div>
    )
}

start.story = {
    name: 'Start',
}
