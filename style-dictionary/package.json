{
    "name": "style-dictionary",
    "version": "0.1.0",
    "description": "Style once, use everywhere in catwalk.",
    "main": "build/index.js",
    "files": [
        "build",
        "properties"
    ],
    "scripts": {
        "build": "node_modules/.bin/style-dictionary build",
        "clean": "rm -rf build",
        "watch": "npm run build && chokidar 'properties/**/*.json' -c 'npm run build'",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "chokidar-cli": "^1.2.0",
        "style-dictionary": "2.8.1"
    }
}